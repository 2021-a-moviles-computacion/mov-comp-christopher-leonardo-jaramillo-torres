<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gracegbe.proyecto_2b">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proyecto2B">
        <activity android:name=".InicioRestaurante"></activity>
        <activity android:name=".DatosReservacion" />
        <activity android:name=".VistaRestaurante" />
        <activity android:name=".InicioCliente" />
        <activity android:name=".DatosRestaurante" />
        <activity android:name=".DatosCliente" />
        <activity android:name=".RegistroPrincipal" />
        <activity android:name=".InicioAplicacion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>